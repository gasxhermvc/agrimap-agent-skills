{
  "schemaVersion": 3,
  "operations": [
    {
      "name": "agm-analyze",
      "operation": "analyze",
      "visibility": "compatibility",
      "replacedBy": "agm-fe|agm-be|agm-sql action=analyze",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Analyze scope, hidden problems, impacts, and trade-offs",
      "mode": "product-read-only",
      "deliverable": "direct analysis response at light; tracked analysis artifact only at standard/regulated",
      "requiredInputs": ["objective or a pointed target"],
      "conditionalInputs": ["target_kind when repository evidence cannot resolve the affected discipline"],
      "example": "$agm-analyze depth=light target_files=src/orders.ts objective=\"Explain duplicate requests; do not edit\"",
      "instructions": ["Inspect the full target and affected callers before conclusions. Database-linked conclusions require actual project code, the applicable db-schema, and representative local examples/data shapes; otherwise label the result preliminary and list the missing evidence without guessing or connecting to a database.", "Return CLI-readable plain Markdown in this order: Scope, Evidence, Findings, Impacts, Options, Recommendation, Unknowns. Use short bullets, no wide tables or raw JSON/YAML dumps, and never edit product artifacts."],
      "references": [
        { "path": "analysis-discipline.md", "why": "evidence labels and reasoning quality" },
        { "path": "input-and-scope.md", "why": "target and input coverage" }
      ],
      "conditionalReferences": [
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend analysis fundamentals" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend analysis fundamentals including request-value normalization" },
        { "when": "the backend target contains C#", "path": "patterns/csharp.md", "why": "project-wide C# shape, naming, boundary, and code examples" },
        { "when": "the backend target reads cookie, header, query, form, JSON body, or device ID", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "exact normalize API and compatibility behavior" }
      ]
    },
    {
      "name": "agm-diagnose",
      "operation": "diagnose",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Diagnose a problem to a proven root cause",
      "mode": "product-read-only",
      "deliverable": "concise tracked diagnosis task at light; expanded diagnosis artifact at standard/regulated",
      "requiredInputs": ["symptom"],
      "conditionalInputs": ["reproduction evidence when the symptom is not observable from the repository"],
      "example": "$agm-diagnose depth=light symptom=\"Save returns 500\" target_files=src/orders.ts implementation=false",
      "instructions": ["Reproduce or trace the symptom, separate causes from correlations, and test bounded hypotheses.", "Return the proven root cause, evidence, affected path, and proposed fix; do not apply the fix."],
      "references": [
        { "path": "analysis-discipline.md", "why": "hypothesis and evidence discipline" },
        { "path": "input-and-scope.md", "why": "symptom and target coverage" }
      ],
      "conditionalReferences": [
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend diagnosis fundamentals" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend diagnosis fundamentals including request-value normalization" },
        { "when": "the backend target contains C#", "path": "patterns/csharp.md", "why": "project-wide C# behavior and style baseline" },
        { "when": "the backend symptom may involve cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "expected extraction, normalization, and precedence" }
      ]
    },
    {
      "name": "agm-simulate",
      "operation": "simulate",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Simulate scenarios, risks, and observable outcomes",
      "mode": "product-read-only",
      "deliverable": "direct simulation at light; tracked simulation artifact only at standard/regulated",
      "requiredInputs": ["scenario"],
      "conditionalInputs": ["inputs and observables when they cannot be derived from the scenario"],
      "example": "$agm-simulate depth=light scenario=\"Retry after timeout\" inputs=timeout,retry-count",
      "instructions": ["Model the initial state, transitions, branches, failure modes, and observables.", "Label assumptions and confidence; do not present a simulation as measured runtime fact."],
      "references": [
        { "path": "analysis-discipline.md", "why": "assumption and confidence labels" }
      ],
      "conditionalReferences": [
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend state and observable fundamentals" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend boundary and runtime fundamentals" },
        { "when": "the backend scenario contains C#", "path": "patterns/csharp.md", "why": "project-wide C# behavior and style baseline" },
        { "when": "the backend scenario involves cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value transition and precedence behavior" }
      ]
    },
    {
      "name": "agm-plan",
      "operation": "plan",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Create a reverse-engineered execution plan",
      "mode": "product-read-only",
      "deliverable": "direct plan at light; tracked plan artifact only at standard/regulated",
      "requiredInputs": ["objective"],
      "conditionalInputs": ["decision-owner choice when multiple material approaches remain viable"],
      "example": "$agm-plan depth=light objective=\"Add order cancellation\" target_files=src/orders.ts",
      "instructions": ["Reverse-engineer current behavior, dependencies, callers, tests, and integration order before planning.", "Each step names target, action, reason, verification, and decision-owner gate; do not implement."],
      "references": [
        { "path": "analysis-discipline.md", "why": "evidence-backed plan assumptions" },
        { "path": "input-and-scope.md", "why": "scope ledger" }
      ],
      "conditionalReferences": [
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend execution-plan fundamentals" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend execution-plan fundamentals" },
        { "when": "the backend plan contains C#", "path": "patterns/csharp.md", "why": "project-wide C# implementation baseline" },
        { "when": "the backend plan touches cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value API and precedence plan" }
      ]
    },
    {
      "name": "agm-design",
      "operation": "design",
      "visibility": "compatibility",
      "replacedBy": "agm-fe|agm-be|agm-sql action=design, or passive design in agm-create-prompt",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Design a user flow, behavior, and acceptance criteria",
      "mode": "product-read-only",
      "deliverable": "direct design at light; tracked design artifact only at standard/regulated",
      "requiredInputs": ["objective"],
      "conditionalInputs": ["target_kind when FE/BE placement remains ambiguous", "phase for FE/BE work when repository evidence cannot resolve it"],
      "example": "$agm-design depth=light target_kind=fe-main objective=\"Design empty/loading/error states\"",
      "instructions": ["Define actors, entry points, states, transitions, validation, failure/recovery, accessibility, and measurable acceptance.", "Use the target discipline selected by the conditional routing below; do not implement."],
      "references": [
        { "path": "analysis-discipline.md", "why": "facts versus proposed design" },
        { "path": "input-and-scope.md", "why": "design reference coverage" }
      ],
      "conditionalReferences": [
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend design discipline" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend boundary discipline" },
        { "when": "the backend design contains C#", "path": "patterns/csharp.md", "why": "project-wide C# contract and boundary examples" },
        { "when": "the backend design includes cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value resolution contract" }
      ]
    },
    {
      "name": "agm-architect",
      "operation": "architect",
      "depth": { "default": "standard", "allowed": ["standard", "regulated"] },
      "description": "Design boundaries, contracts, and migration trade-offs",
      "mode": "product-read-only",
      "deliverable": ".agrimap-agent/decisions/<decision-id>.md",
      "requiredInputs": ["boundary question or objective"],
      "conditionalInputs": ["decision-owner approval before an architecture record becomes approved"],
      "example": "$agm-architect requested_by=Billy objective=\"Choose ownership boundary for order status\"",
      "instructions": ["Map ownership, contracts, data flow, runtime/deployment effects, alternatives, migration, rollback, and reversible decisions.", "Produce a proposed decision record; only recorded authority may approve it."],
      "references": [
        { "path": "analysis-discipline.md", "why": "architecture evidence and counterarguments" },
        { "path": "service-ownership.md", "why": "canonical service and data ownership" }
      ],
      "conditionalReferences": [
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend architecture fundamentals" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend architecture fundamentals" },
        { "when": "the backend architecture contains C#", "path": "patterns/csharp.md", "why": "project-wide C# structure baseline" },
        { "when": "the backend architecture includes cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value ownership and precedence contract" }
      ]
    },
    {
      "name": "agm-review",
      "operation": "review",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Review code or artifacts with evidence-backed findings",
      "mode": "product-read-only",
      "deliverable": "direct findings at light; tracked review artifact only at standard/regulated",
      "requiredInputs": ["target"],
      "conditionalInputs": ["review_scope when the requester wants narrower coverage than the default"],
      "example": "$agm-review depth=light target_files=src/orders.ts review_scope=correctness,regression,tests",
      "instructions": ["Review correctness, regressions, contracts/data, maintainability, relevant performance, and tests in that order.", "Report actionable findings by severity with file/line, evidence, and impact; do not fix them."],
      "references": [
        { "path": "analysis-discipline.md", "why": "finding evidence and severity" }
      ],
      "conditionalReferences": [
        { "when": "the target is FE, BE, or SQL", "path": "patterns/pattern-status.md", "why": "route to the current target pattern only" },
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend review fundamentals" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend review fundamentals" },
        { "when": "the backend review contains C#", "path": "patterns/csharp.md", "why": "project-wide C# review baseline" },
        { "when": "the backend review touches cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value correctness and precedence review" }
      ]
    },
    {
      "name": "agm-history",
      "operation": "history",
      "depth": { "default": "light", "allowed": ["light"] },
      "description": "Query workflow attribution and audit-storage status by person, task, event, or UTC time range",
      "mode": "product-read-only",
      "deliverable": "structured history result",
      "requiredInputs": ["at least one useful person, task, event, or time-range query"],
      "conditionalInputs": ["explicit UTC offset for timestamp inputs"],
      "example": "$agm-history requester=Billy days=5",
      "instructions": ["Run the workspace history command and answer from versioned workflow evidence, not conversational recall.", "Separate requester, execution identity, claimed files, Git authorship, and storage durability.", "Remain at light depth: never identify, start, checkpoint, or create task artifacts for a history query."],
      "references": [
        { "path": "memory-and-logs.md", "why": "history filters and attribution semantics" }
      ]
    },
    {
      "name": "agm-fe",
      "operation": "fe",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Analyze, design, create, edit, or explicitly test frontend work through one domain façade",
      "mode": "action-routed",
      "deliverable": "action-specific frontend analysis, design, bounded implementation, or selected tests",
      "requiredInputs": ["one action or unambiguous natural-language intent", "an objective or pointed frontend target"],
      "conditionalInputs": ["phase and target_kind when repository evidence cannot resolve them", "requester selection when passive test advice finds unspecified coverage"],
      "example": "$agm-fe action=edit target_files=src/order.component.ts objective=\"Handle the empty state\"",
      "actions": [
        { "name": "analyze", "mode": "product-read-only", "activation": "explicit-or-passive", "depths": ["light", "standard", "regulated"], "purpose": "Inspect frontend scope, behavior, callers, risks, and trade-offs without editing" },
        { "name": "design", "mode": "product-read-only", "activation": "explicit-or-passive", "depths": ["light", "standard", "regulated"], "purpose": "Define frontend flow, states, accessibility, validation, and acceptance without editing" },
        { "name": "create", "mode": "product-write", "activation": "explicit", "depths": ["light"], "purpose": "Create a bounded new frontend slice" },
        { "name": "edit", "mode": "product-write", "activation": "explicit", "depths": ["light"], "purpose": "Modify a bounded existing frontend slice" },
        { "name": "test", "mode": "product-write", "activation": "explicit", "depths": ["light"], "purpose": "Create explicitly requested or selected frontend tests after passive test advice" }
      ],
      "instructions": ["Resolve exactly one action before inspection. Explicit action= wins; otherwise infer only from unambiguous verbs and report the resolved action. Ask one short question when create versus edit or read versus write remains ambiguous.", "For analyze/design, never edit product files. For create/edit/test, show the bounded slice, stay within light limits, and route broader work to agm-create-prompt before any product write.", "Run the passive test advisor during analyze, design, create, and edit. Suggest prioritized coverage but never create tests unless action=test or the requester explicitly asked to create tests."],
      "references": [
        { "path": "passive-capabilities.md", "why": "passive design and test-advisor boundaries" },
        { "path": "elicitation.md", "why": "action, scope, and propose-first resolution" },
        { "path": "frontend-engineer.md", "why": "frontend phase discipline" },
        { "path": "patterns/frontend.md", "why": "current frontend contract" }
      ]
    },
    {
      "name": "agm-be",
      "operation": "be",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Analyze, design, create, edit, or explicitly test backend work through one domain façade",
      "mode": "action-routed",
      "deliverable": "action-specific backend analysis, design, bounded implementation, or selected tests",
      "requiredInputs": ["one action or unambiguous natural-language intent", "an objective or pointed backend target"],
      "conditionalInputs": ["target_kind, backend_profile, and phase when repository evidence cannot resolve them", "requester selection when passive test advice finds unspecified coverage"],
      "example": "$agm-be action=create target_kind=be-main backend_profile=agmws objective=\"Add cancel-order endpoint\"",
      "actions": [
        { "name": "analyze", "mode": "product-read-only", "activation": "explicit-or-passive", "depths": ["light", "standard", "regulated"], "purpose": "Inspect backend scope, contracts, callers, data, risks, and trade-offs without editing" },
        { "name": "design", "mode": "product-read-only", "activation": "explicit-or-passive", "depths": ["light", "standard", "regulated"], "purpose": "Define backend boundaries, flow, validation, failure behavior, and acceptance without editing" },
        { "name": "create", "mode": "product-write", "activation": "explicit", "depths": ["light"], "purpose": "Create a bounded new backend slice" },
        { "name": "edit", "mode": "product-write", "activation": "explicit", "depths": ["light"], "purpose": "Modify a bounded existing backend slice" },
        { "name": "test", "mode": "product-write", "activation": "explicit", "depths": ["light"], "purpose": "Create explicitly requested or selected backend tests after passive test advice" }
      ],
      "instructions": ["Resolve exactly one action before inspection. Explicit action= wins; otherwise infer only from unambiguous verbs and report the resolved action. Ask one short question when create versus edit or read versus write remains ambiguous.", "For analyze/design, never edit product files. For create/edit/test, show the bounded slice, stay within light limits, and route broader work to agm-create-prompt before any product write.", "Run the passive test advisor during analyze, design, create, and edit. Suggest prioritized coverage but never create tests unless action=test or the requester explicitly asked to create tests."],
      "references": [
        { "path": "passive-capabilities.md", "why": "passive design and test-advisor boundaries" },
        { "path": "elicitation.md", "why": "action, scope, and propose-first resolution" },
        { "path": "backend-engineer.md", "why": "backend phase and profile discipline" },
        { "path": "patterns/backend.md", "why": "current backend contract" }
      ],
      "conditionalReferences": [
        { "when": "the backend target contains C#", "path": "patterns/csharp.md", "why": "project-wide C# baseline" },
        { "when": "the target reads cookie, header, query, form, JSON body, or device ID", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value behavior and compatibility" }
      ]
    },
    {
      "name": "agm-sql",
      "operation": "sql",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Analyze, design, create, edit, or explain SQL work through one domain façade",
      "mode": "action-routed",
      "deliverable": "action-specific SQL analysis, design, explanation, or bounded SQL implementation",
      "requiredInputs": ["one action or unambiguous natural-language intent", "an objective or pointed SQL target"],
      "conditionalInputs": ["target_kind when table/procedure placement cannot be resolved", "owner evidence for persisted-data or contract decisions"],
      "example": "$agm-sql action=explain target_files=sql/ORDER/ORDER_Q.sql",
      "actions": [
        { "name": "analyze", "mode": "product-read-only", "activation": "explicit-or-passive", "depths": ["light", "standard", "regulated"], "purpose": "Inspect SQL scope, schema, callers, contracts, risks, and trade-offs without editing" },
        { "name": "design", "mode": "product-read-only", "activation": "explicit-or-passive", "depths": ["light", "standard", "regulated"], "purpose": "Define SQL objects, data flow, result and error contracts, deployment, and acceptance without editing" },
        { "name": "create", "mode": "product-write", "activation": "explicit", "depths": ["light"], "purpose": "Create a bounded new SQL object or slice" },
        { "name": "edit", "mode": "product-write", "activation": "explicit", "depths": ["light"], "purpose": "Modify a bounded existing SQL object or slice" },
        { "name": "explain", "mode": "product-read-only", "activation": "explicit-or-passive", "depths": ["light", "standard", "regulated"], "purpose": "Explain SQL purpose, contracts, reads/writes, flow, transactions, errors, assumptions, and risks without execution or edits" }
      ],
      "instructions": ["Resolve exactly one action before inspection. Explicit action= wins; otherwise infer only from unambiguous verbs and report the resolved action. Ask one short question when create versus edit or read versus write remains ambiguous.", "For analyze/design/explain, never execute SQL, connect to a database, or edit product files. Explain uses FACT, INFERENCE, and UNKNOWN and routes requested modifications to edit.", "For create/edit, show the bounded slice, stay within light limits, load applicable schema/caller evidence, and route broader work to agm-create-prompt before any product write."],
      "references": [
        { "path": "passive-capabilities.md", "why": "passive design and SQL explain boundaries" },
        { "path": "elicitation.md", "why": "action and scope resolution" },
        { "path": "patterns/sql.md", "why": "current SQL contract, formatting, and validation" }
      ]
    },
    {
      "name": "agm-refactor",
      "operation": "refactor",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Refactor FE, BE, or SQL through one target-routed command with an explicit behavior mode",
      "mode": "product-write",
      "deliverable": "bounded target-specific changes with preserved or explicitly authorized behavior and proportional verification",
      "requiredInputs": ["target=fe|be|sql", "target files or objective", "refactor_mode"],
      "conditionalInputs": ["backend_profile for be-main", "decision-owner approval for logic, contract, data, or result-shape change"],
      "example": "$agm-refactor target=sql refactor_mode=performance-preserve-behavior target_files=sql/usp_Order_Search.sql",
      "instructions": ["Resolve exactly one target discipline and one refactor mode before editing; when mode is ambiguous, show all five modes with one recommendation.", "Dispatch to only the matching FE, BE, or SQL discipline. Preserve behavior except where the selected mode and recorded authority explicitly allow a change.", "For SQL, preserve result sets, transactions, side effects, error mapping, and deployment idempotency, then format and validate every declared changed SQL path."],
      "references": [
        { "path": "refactor-modes.md", "why": "allowed behavior-change boundary" },
        { "path": "elicitation.md", "why": "target and mode resolution" }
      ],
      "conditionalReferences": [
        { "when": "target=fe", "path": "frontend-engineer.md", "why": "frontend refactor discipline" },
        { "when": "target=fe", "path": "patterns/frontend.md", "why": "current frontend contract" },
        { "when": "target=be", "path": "backend-engineer.md", "why": "backend refactor discipline" },
        { "when": "target=be", "path": "patterns/backend.md", "why": "current backend contract" },
        { "when": "the backend target contains C#", "path": "patterns/csharp.md", "why": "project-wide C# refactor baseline" },
        { "when": "the backend refactor touches request values or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value behavior and precedence" },
        { "when": "target=sql", "path": "patterns/sql.md", "why": "current SQL refactor, formatting, and validation contract" }
      ]
    },
    {
      "name": "agm-refactor-fe",
      "operation": "refactor-fe",
      "visibility": "compatibility",
      "replacedBy": "agm-refactor target=fe",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Refactor frontend code using an explicit behavior mode",
      "mode": "product-write",
      "deliverable": "bounded frontend changes plus direct verified result at light or schema result at standard/regulated",
      "requiredInputs": ["target", "refactor_mode"],
      "conditionalInputs": ["target_kind and phase when repository evidence cannot resolve them", "decision-owner approval for logic change"],
      "example": "$agm-refactor-fe depth=light target_kind=fe-main refactor_mode=strict-preserve-logic target_files=src/order.component.ts",
      "instructions": ["Prove current behavior and select exactly one refactor mode before editing.", "Preserve or change behavior only as that mode allows; run frontend pattern gates and proportional verification."],
      "references": [
        { "path": "refactor-modes.md", "why": "allowed behavior-change boundary" },
        { "path": "frontend-engineer.md", "why": "frontend phase discipline" },
        { "path": "patterns/frontend.md", "why": "current frontend contract" }
      ]
    },
    {
      "name": "agm-refactor-be",
      "operation": "refactor-be",
      "visibility": "compatibility",
      "replacedBy": "agm-refactor target=be",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Refactor backend code using an explicit behavior mode",
      "mode": "product-write",
      "deliverable": "bounded backend changes plus direct verified result at light or schema result at standard/regulated",
      "requiredInputs": ["target", "refactor_mode"],
      "conditionalInputs": ["backend_profile for be-main", "decision-owner approval for logic or contract change"],
      "example": "$agm-refactor-be depth=light target_kind=be-main backend_profile=agmws refactor_mode=strict-preserve-logic target_files=Application/Orders.cs",
      "instructions": ["Prove current behavior and select exactly one refactor mode before editing.", "Preserve architecture, routes, responses, messages, data, and public contracts unless authority explicitly approves change."],
      "references": [
        { "path": "refactor-modes.md", "why": "allowed behavior-change boundary" },
        { "path": "backend-engineer.md", "why": "backend phase and profile discipline" },
        { "path": "patterns/backend.md", "why": "current backend contract" }
      ],
      "conditionalReferences": [
        { "when": "the backend refactor contains C#", "path": "patterns/csharp.md", "why": "project-wide C# refactor baseline" },
        { "when": "the refactor touches direct request/header/cookie/query/form/body access or repeated blank/trim/fallback logic", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "behavior-preserving normalization target for main/library code" }
      ]
    },
    {
      "name": "agm-refactor-sql",
      "operation": "refactor-sql",
      "visibility": "compatibility",
      "replacedBy": "agm-refactor target=sql",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Refactor SQL using an explicit behavior mode",
      "mode": "product-write",
      "deliverable": "bounded SQL changes plus direct verified result at light or schema result at standard/regulated",
      "requiredInputs": ["target", "refactor_mode=performance-preserve-behavior|readability-organization|strict-preserve-logic|strict-allow-logic-change|targeted-bug-fix"],
      "conditionalInputs": ["decision-owner approval for data, result-shape, transaction, or error-contract change"],
      "example": "$agm-refactor-sql depth=light target_kind=sql-procedure refactor_mode=strict-preserve-logic target_files=sql/usp_Order_Search.sql",
      "instructions": ["Load matching schema facts and select one mode. If ambiguous, show all five enums with one-line boundaries and mark one recommendation in the first reply; never return a recommendation alone.", "Preserve result sets, transactions, side effects, error mapping, and deployment idempotency unless authorized otherwise.", "After all SQL writes, format every declared changed .sql path, validate the same set, and report formatted N/N. Only missing sqlfluff triggers install-sqlfluff.mjs plus one retry; other failures never install. Folder nonzero follows patterns/sql.md recovery."],
      "references": [
        { "path": "refactor-modes.md", "why": "allowed behavior-change boundary" },
        { "path": "patterns/sql.md", "why": "current SQL contract and message gate" }
      ]
    },
    {
      "name": "agm-qa",
      "operation": "qa",
      "depth": { "default": "light", "allowed": ["light", "regulated"] },
      "description": "Verify an artifact under a product-read-only, execution-restricted QA contract",
      "mode": "verification-only",
      "deliverable": "direct QA evidence at light; .agrimap-agent/tasks/<task-id>/qa.md only at regulated",
      "requiredInputs": ["artifact or target to verify"],
      "conditionalInputs": ["task_id for regulated tracked QA", "qa_mode=full only when an explicit full-QA trigger applies"],
      "example": "$agm-qa depth=light qa_mode=light artifact=\"integrated workspace\"",
      "instructions": ["Default to depth=light and qa_mode=light; a provider/model, SQL/BE/FE target, regulated implementation, or large diff never selects full by itself.", "Apply qa-and-done.md, keep product artifacts read-only, and use only read-only inspection plus read-only AgriMap script paths. Never format SQL, install SQLFluff, or connect to LocalDB, dbserver, SQL Server, or another runtime service.", "The only product-command exceptions are dotnet build for an existing BE project when compile evidence is necessary, and npm run start:agrimap:development for FE only at qa_mode=full when startup evidence is explicitly necessary; do not run other product commands.", "Return only canonical status, evidence, commands actually run, and limitations."],
      "references": [
        { "path": "qa-and-done.md", "why": "single QA, correction, and completion policy" }
      ],
      "conditionalReferences": [
        { "when": "the task is FE, BE, or SQL", "path": "patterns/pattern-status.md", "why": "select exactly the matching pattern and Detect gates" },
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend QA fundamentals" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend QA fundamentals including request-value normalization" },
        { "when": "the backend artifact contains C#", "path": "patterns/csharp.md", "why": "project-wide C# QA baseline" },
        { "when": "BE work reads or refactors cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value QA behavior matrix for main/library code" }
      ]
    },
    {
      "name": "agm-create-unit-test",
      "operation": "create-unit-test",
      "visibility": "compatibility",
      "replacedBy": "agm-fe|agm-be action=test; SQL verification stays under agm-sql",
      "depth": { "default": "light", "allowed": ["light", "standard", "regulated"] },
      "description": "Create target-specific unit or regression tests",
      "mode": "product-write",
      "deliverable": "selected tests plus direct verified result at light or schema result at standard/regulated",
      "requiredInputs": ["target"],
      "conditionalInputs": ["requester selection when behaviors were not already specified", "backend_profile for be-main"],
      "example": "$agm-create-unit-test depth=light target_files=src/orders.ts objective=\"cover duplicate cancellation\"",
      "instructions": ["Inspect the existing test framework and propose a numbered risk/behavior list when coverage was not specified.", "Create only selected tests and follow the matching target discipline and naming."],
      "references": [
        { "path": "elicitation.md", "why": "propose-first selection contract" }
      ],
      "conditionalReferences": [
        { "when": "target is FE", "path": "frontend-engineer.md", "why": "frontend test discipline" },
        { "when": "target is BE", "path": "backend-engineer.md", "why": "backend test discipline" },
        { "when": "the backend test target contains C#", "path": "patterns/csharp.md", "why": "project-wide C# test style and required cases" },
        { "when": "the backend test target touches cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value behavior matrix for tests" },
        { "when": "target is SQL", "path": "patterns/sql.md", "why": "SQL verification boundary" }
      ]
    },
    {
      "name": "agm-create-feature",
      "operation": "create-feature",
      "visibility": "compatibility",
      "replacedBy": "agm-fe|agm-be|agm-sql action=create|edit",
      "depth": { "default": "light", "allowed": ["light"] },
      "description": "Create a bounded FE, BE, batch, library, or SQL feature",
      "mode": "product-write",
      "deliverable": "confirmed bounded vertical slice plus concise tracked brief, checklist, memory, logs, and verified result; no separate QA artifact",
      "requiredInputs": ["objective"],
      "conditionalInputs": ["target_kind if placement is unresolved", "backend_profile for be-main", "agm-create-prompt handoff beyond light or for a new scaffold"],
      "example": "$agm-create-feature depth=light target_kind=be-main backend_profile=agmws objective=\"Add cancel-order endpoint\"",
      "instructions": ["This operation is light/direct only: start concise tracked task state and write the mandatory .agrimap-agent brief, checklist, memory, logs, and closure result; never invoke separate QA, select qa_mode, delegate/spawn/wait, or create prompts.", "Propose the smallest complete slice and output paths before writing; after confirmation, implement and verify proportionally.", "For separate QA, an unresolved or material persisted-data decision, standard/regulated depth, more than three product artifacts, material owner decisions, or a new scaffold, stop before writes and route the work to agm-create-prompt; never partially execute.", "Return the result only after product writes and verification finish; never pre-write a result artifact.", "Use the matching target discipline.", "For direct SQL, a bounded slice within the three-artifact limit remains direct only with resolved owner/golden/schema/caller evidence. Run sql-contract-preflight.mjs per object and load returned paths. Use [agrimap_app], canonical files, guarded messages.sql inserts, and the SQLFluff format-then-validate gate. Do not hand-tune cosmetic layout: format every declared changed .sql path, validate the same complete set, and report formatted N/N. Never use a database, ScriptDom, or other parser/runtime."],
      "references": [
        { "path": "elicitation.md", "why": "placement, naming, and scaffold gates" }
      ],
      "conditionalReferences": [
        { "when": "target is FE", "path": "frontend-engineer.md", "why": "frontend feature discipline" },
        { "when": "target is BE", "path": "backend-engineer.md", "why": "backend feature discipline" },
        { "when": "the backend target contains C#", "path": "patterns/csharp.md", "why": "C# baseline" },
        { "when": "the backend target reads request values or resolves device ID", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request priority" },
        { "when": "target is SQL", "path": "patterns/sql.md", "why": "SQL creation and deployment discipline" }
      ]
    },
    {
      "name": "agm-create-prompt",
      "operation": "create-prompt",
      "depth": { "default": "regulated", "allowed": ["regulated"] },
      "description": "Create provider-specific Leader, executor, and QA prompts",
      "mode": "workflow-write-only",
      "deliverable": "tracked brief.md and acceptance checklist.md plus .agrimap-agent/prompts/<task-id>/<role>.prompt.md; no implementation, qa.md, or result.md",
      "requiredInputs": ["objective", "provider"],
      "conditionalInputs": ["target_kind/backend_profile for target-specific work", "decision-owner approval before execution"],
      "example": "$agm-create-prompt requested_by=Billy provider=codex objective=\"Delegate cancel-order implementation\"",
      "instructions": ["Own the tracked feature contract: run staged elicitation, write the approved brief.md and acceptance checklist.md, build one execution SoT per task, and create separate Leader/executor/QA prompts when implementation is delegated.", "Generation ends in draft and a non-terminal scope-decision checkpoint; never execute the generated prompt, create qa.md, create result.md, or complete the implementation task in this operation."],
      "references": [
        { "path": "create-prompt.md", "why": "prompt lifecycle and provider rendering" },
        { "path": "passive-capabilities.md", "why": "passive design discipline used while shaping the approved brief and acceptance" },
        { "path": "subagents-and-branches.md", "why": "delegation, visibility, and integration" },
        { "path": "model-capability-matrix.yaml", "why": "capability-profile routing labels" }
      ],
      "conditionalReferences": [
        { "when": "target_kind is fe-main or fe-library", "path": "frontend-engineer.md", "why": "frontend fundamentals that every generated handoff must carry" },
        { "when": "target_kind is be-main or be-library", "path": "backend-engineer.md", "why": "backend fundamentals that every generated handoff must carry" },
        { "when": "the backend handoff contains C#", "path": "patterns/csharp.md", "why": "project-wide C# rules that every generated handoff must carry" },
        { "when": "the backend handoff touches cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value rules that every generated handoff must carry" },
        { "when": "target_kind is sql-table, sql-procedure, or sql-table-and-procedure", "path": "patterns/sql.md", "why": "SQL fundamentals that every generated handoff must carry" }
      ]
    },
    {
      "name": "agm-exec",
      "operation": "execute",
      "depth": { "default": "regulated", "allowed": ["regulated"] },
      "description": "Execute one decision-owner-approved generated prompt as the execution source of truth with checkpoints, deviation stops, and a Result Package",
      "mode": "product-write",
      "deliverable": "integrated Result Package awaiting canonical QA",
      "requiredInputs": ["approved prompt path or resumable task_id"],
      "conditionalInputs": ["decision-owner choice when a material deviation exceeds the approved prompt"],
      "example": "$agm-exec requested_by=Billy prompt=.agrimap-agent/prompts/<task-id>/executor.prompt.md",
      "instructions": ["Require prompt_status=owner-approved and execute its ordered steps as the task source of truth.", "Stop and report structured deviation evidence when a material change exceeds scope; return the Result Package and integration artifact.", "Own implementation only: update the acceptance checklist and record integration evidence, but do not create qa.md or result.md; QA and final Leader closure are later phases."],
      "references": [
        { "path": "create-prompt.md", "why": "prompt SoT and deviation contract" },
        { "path": "subagents-and-branches.md", "why": "native activity, workspace, and integration contract" },
        { "path": "qa-and-done.md", "why": "regulated QA and completion source" }
      ],
      "conditionalReferences": [
        { "when": "the approved prompt targets C# backend work", "path": "backend-engineer.md", "why": "backend discipline carried into execution" },
        { "when": "the approved prompt targets C# backend work", "path": "patterns/csharp.md", "why": "project-wide C# execution baseline" },
        { "when": "the approved backend prompt touches cookie, header, query, form, JSON body, or device-ID resolution", "path": "patterns/golden/backend-libraries/013-1-extensions-request-value-normalize.md", "why": "request-value execution contract" }
      ]
    }
  ]
}
