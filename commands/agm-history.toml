description = "Query workflow attribution and audit-storage status by person, task, event, or UTC time range"
prompt = "Run only AgriMap operation history through its compact progressive-disclosure entrypoint.\n\nRead only skills/agrimap-agent-skills/references/runtime-core.md, references/glossary.md, and references/operations/history.md first; do not load the routing SKILL.md or combine another operation.\n\nThe operation entrypoint names any additional conditional references. Treat those compact files as the workflow source of trust. If one is missing or corrupt, stop with PACKAGE_ENTRYPOINT_MISSING; never fall back to the router.\n\nWhen requester arguments contain a standalone -h or --help token, return compact operation help without starting a task or writing project state.\n\nRequester arguments:\n\n{{args}}"
