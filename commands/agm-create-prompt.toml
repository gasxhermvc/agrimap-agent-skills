description = "Create provider-specific Leader, executor, and QA prompts"
prompt = "Run only AgriMap operation create-prompt through its compact progressive-disclosure entrypoint.\n\nRead exactly skills/agrimap-agent-skills/references/lifecycle-core.md and references/operations/create-prompt.md first; do not preload the glossary, routing SKILL.md, or another operation.\n\nThe operation entrypoint names any additional conditional references. Treat those compact files as the workflow source of trust. If one is missing or corrupt, stop with PACKAGE_ENTRYPOINT_MISSING; never fall back to the router.\n\nWhen requester arguments contain a standalone -h or --help token, return compact operation help without starting a task or writing project state.\n\nRequester arguments:\n\n{{args}}"
