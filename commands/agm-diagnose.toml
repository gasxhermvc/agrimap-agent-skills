description = "Diagnose a problem to a proven root cause"
prompt = "Run only AgriMap operation diagnose through its compact progressive-disclosure entrypoint.\n\nRead only skills/agrimap-agent-skills/references/runtime-core.md, references/glossary.md, and references/operations/diagnose.md first; do not load the routing SKILL.md or combine another operation.\n\nThe operation entrypoint names any additional conditional references. Treat those compact files as the workflow source of trust. If one is missing or corrupt, stop with PACKAGE_ENTRYPOINT_MISSING; never fall back to the router.\n\nWhen requester arguments contain a standalone -h or --help token, return compact operation help without starting a task or writing project state.\n\nRequester arguments:\n\n{{args}}"
